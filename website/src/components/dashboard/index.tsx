import { FC } from "react";

const Dashboard: FC = () => {
  return (
    <div className="p-grid">
      <div className="p-col">
        PRIVATE ROUTE
      </div>
    </div>
  );
};

export default Dashboard;
